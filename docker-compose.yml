services:
  postgres:
    image: postgres:15-alpine
    container_name: agora-worker-postgres
    environment:
      POSTGRES_DB: agora
      POSTGRES_USER: agora
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-localpassword}
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U agora"]
      interval: 5s
      timeout: 5s
      retries: 5

  redis:
    image: redis:7-alpine
    container_name: agora-worker-redis
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 5s
      timeout: 3s
      retries: 5

  worker:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: agora-worker
    environment:
      DATABASE_URL: postgresql://agora:${POSTGRES_PASSWORD:-localpassword}@postgres:5432/agora
      REDIS_URL: redis://redis:6379/0
      AWS_REGION: ${AWS_REGION:-us-east-1}
      SQS_QUEUE_URL: ${SQS_QUEUE_URL:-}
      BEDROCK_MODEL_ID: anthropic.claude-3-5-sonnet-20241022-v2:0
      SECRET_KEY: ${SECRET_KEY:-local-dev-secret-key-change-in-prod}
      AWS_ACCESS_KEY_ID: ${AWS_ACCESS_KEY_ID}
      AWS_SECRET_ACCESS_KEY: ${AWS_SECRET_ACCESS_KEY}
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy

  consumer:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: agora-consumer
    entrypoint: ["python", "-m", "app.sqs_consumer"]
    environment:
      DATABASE_URL: postgresql://agora:${POSTGRES_PASSWORD:-localpassword}@postgres:5432/agora
      REDIS_URL: redis://redis:6379/0
      AWS_REGION: ${AWS_REGION:-us-east-1}
      SQS_QUEUE_URL: ${SQS_QUEUE_URL:-}
      BEDROCK_MODEL_ID: anthropic.claude-3-5-sonnet-20241022-v2:0
      SECRET_KEY: ${SECRET_KEY:-local-dev-secret-key-change-in-prod}
      AWS_ACCESS_KEY_ID: ${AWS_ACCESS_KEY_ID}
      AWS_SECRET_ACCESS_KEY: ${AWS_SECRET_ACCESS_KEY}
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy

networks:
  default:
    name: agora-worker-network
